#include <stdio.h>
int main(){
    int number, numberDigits,original, rem,sum=0;
    printf("Enter Number: ");
    scanf("%d", &number);
    original = number;
    while(number!=0){
        rem = number%10;


    }
    printf("%d", number);
    return 0;

}
//number that equals the sum of its digits each raised to a power.
